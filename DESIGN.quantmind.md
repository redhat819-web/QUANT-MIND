# DESIGN.quantmind.md — QUANT-MIND 디자인 오버레이

Kraken `DESIGN.md` 위에 얹는 프로젝트 전용 규칙. 이 문서는 **원본에서 바꿔야 하는 것과 원본에 없는 것**만 담는다. 원본에 이미 있고 충돌하지 않는 규칙은 여기 다시 쓰지 않는다.

---

## 0. 문서 구조와 우선순위

```
DESIGN.md              Kraken 원본. 기본 시각 언어. 수정 금지
DESIGN.quantmind.md    이 문서. 프로젝트 규칙. DESIGN.md보다 우선
spec.md                어떤 화면과 데이터를 보여줄 것인가
plan.md                React/Vite, Supabase, 차트로 어떻게 구현할 것인가
```

**적용 순서**

1. 이 문서에 규칙이 있으면 이 문서를 따른다.
2. 없으면 `DESIGN.md`를 따른다.
3. 둘 다 없으면 `plan.md`에서 결정하고 그 결정을 이 문서에 추가한다.

`DESIGN.md`를 직접 고치지 않는다. 원본을 고치고 싶은 충동이 들면 그 내용은 전부 이 문서로 온다.

---

## 1. Kraken에서 그대로 가져오는 것

수정 없이 원본을 따른다. 구현 시 원본을 직접 참조할 것.

- 흰 배경 기반 표면 체계와 중립 계열 (`#ffffff`, `#dedee5`, `#686b82`, `#9497a9`)
- 본문 텍스트 near-black `#101114`
- 브랜드 보라 `#7132f5` 및 어두운 변형 `#5741d8`, `#5b1ecf`, 16% 투명 보라 배경
- 버튼 5종의 배경·테두리·패딩·반경 규격
- 배지 규격 (성공 / 중립)
- 그림자 2종 (`rgba(0,0,0,0.03) 0 4px 24px`, `rgba(16,24,40,0.04) 0 1px 4px`)
- 반응형 브레이크포인트 (375 / 425 / 640 / 768 / 1024 / 1280 / 1536)
- Do / Don't: 보라를 CTA와 링크에, 버튼 반경 12px, 정의되지 않은 보라 금지

---

## 2. 덮어쓰는 것

### 2.1 등락 색 — 최우선 규칙

Kraken은 크립토 거래소 관행에 따라 초록을 긍정 신호로 쓴다. **이 프로젝트는 국내 시장 관행을 따른다.**

| 의미 | 토큰 | 값 | 비고 |
| --- | --- | --- | --- |
| 상승 | `--qm-rise` | `#D42E3C` | 채움·아이콘·막대 |
| 상승 텍스트 | `--qm-rise-text` | `#B21F2C` | 16px 이하 숫자 |
| 하락 | `--qm-fall` | `#1B6FD4` | 채움·아이콘·막대 |
| 하락 텍스트 | `--qm-fall-text` | `#1558AA` | 16px 이하 숫자 |
| 보합 | `--qm-flat` | `#686b82` | 색을 주지 않는다 |

- Kraken 초록 `#149e61`은 **등락에 절대 쓰지 않는다.** 용도는 "저장 완료", "동기화 성공", "합의 기록됨" 같은 시스템 성공 상태뿐이다.
- 브랜드 보라는 등락 표시에 쓰지 않는다. 보라는 조작(CTA·링크·선택 상태), 빨강/파랑은 시장 데이터. 두 역할이 한 요소에 겹치지 않게 한다.
- 색만으로 등락을 전달하지 않는다. 부호(`+` / `−`)를 항상 표기한다.
- 행 전체나 카드 전체에 등락 배경색을 깔지 않는다. 숫자와 좌측 3px 마커까지만.

### 2.2 서체

Kraken-Brand / Kraken-Product는 전용 폰트라 사용할 수 없고, 한글을 지원하지 않는다.

| 역할 | 대체 서체 | 원본의 어떤 자리를 대신하는가 |
| --- | --- | --- |
| 디스플레이 | Pretendard Variable 700, 음수 자간 | Kraken-Brand |
| UI · 본문 · 숫자 | Pretendard Variable 400/500/600 | Kraken-Product |

- 폴백: `system-ui, -apple-system, "Apple SD Gothic Neo", "Malgun Gothic", sans-serif`
- 라이선스가 불명확한 폰트 파일을 저장소에 넣지 않는다.
- 원본의 음수 자간(-1px / -0.5px)은 라틴 문자 기준이다. **한글 제목에는 -0.02em 이내로 제한한다.** px 단위 음수 자간을 한글에 그대로 적용하면 자소가 붙는다.

### 2.3 행간

원본의 본문 1.38은 영문 기준이다. 한글 장문에서는 읽기 어렵다.

| 대상 | 원본 | 이 프로젝트 |
| --- | --- | --- |
| 본문 16px | 1.38 | **1.6** |
| 결정 기록 본문 17px | — | **1.8** |
| 캡션 14px | 1.43~1.71 | 1.5 |
| 제목류 | 1.17~1.29 | 원본 유지 (한글은 +0.05 허용) |

### 2.4 사용하지 않는 원본 규격

- **Micro 7px uppercase** — 한글에서 판독 불가. 최소 크기는 12px로 둔다.
- **대문자 라벨** — 한글 라벨을 문장형으로 쓴다. 영문 라벨을 대문자로 늘어놓지 않는다.

### 2.5 간격

원본의 간격 목록(1~25px, 13·15·25 포함)은 컴포넌트 실측값이다. 레이아웃 설계에는 쓰지 않는다.

- **컴포넌트 내부**: 원본 실측값을 그대로 쓴다 (버튼 패딩 13px 16px 등).
- **레이아웃 간격**: `8 · 16 · 24 · 32 · 48 · 64`만 쓴다. 그 외 임의 값 금지.

---

## 3. 추가하는 것 — Kraken에 없는 영역

### 3.1 숫자 표기 (이 제품의 핵심 규칙)

- 모든 수치에 `font-variant-numeric: tabular-nums;`. 세로로 자릿수가 어긋나면 안 된다.
- 수치는 **우측 정렬**, 라벨은 좌측 정렬.
- 소수점 자리 고정: 금액은 정수, 비율은 소수 둘째 자리, 수량은 소수 넷째 자리.
- 천 단위 구분 기호 사용. 억 단위 축약(`1.2억`)은 요약 블록에서만 허용하고 표 안에서는 금지.
- 통화 표기를 생략하지 않는다 (`12,480,000 원`, `8,240.50 USD`). KRW와 USD가 한 화면에 있으면 필수.
- 0은 빈칸이 아니라 `0.00%`로 쓴다.
- 평가액 숫자 자체는 `#101114`. **등락색은 변화량과 변화율에만** 준다.

### 3.2 3단 집계 표시

계좌 → 개인 → 가계. 지금 보는 숫자가 어느 단계인지 화면 어디서나 알 수 있어야 한다.

- 상단 바에 집계 단계 전환을 고정 배치하고, 현재 단계를 보라 선택 상태로 표시한다.
- 하위 단계로 내려가도 상위 합계가 화면에서 사라지지 않는다.

### 3.3 데이터 신선도

- 모든 수치 블록에 기준 시각을 12px `#9497a9`로 붙인다.
- 12시간 이상 지난 값은 투명도 70%로 낮추고 기준 시각을 `--qm-stale` (`#8A8F86`)로 바꾼다.
- 동기화 실패 시 화면을 비우지 않는다. 마지막 성공 시점 값을 보여주고 그 사실을 알린다.

### 3.4 이미지 파싱 결과 필드

- 사람이 확인해야 하는 값은 좌측 3px `--qm-warn` (`#B4762A`) 마커로 표시하고, 확인하면 마커를 제거한다.

### 3.5 결정 기록

대시보드 위젯이 아니라 문서로 다룬다.

- 본문 폭 36~44rem, 좌측 정렬, 행간 1.8.
- 상단에 날짜와 상태 배지만. 상태는 Kraken 배지 규격을 쓰되 "합의 완료"는 보라 계열 배지를 쓴다.
- 하단에 작성자와 동의자, 동의 시각을 캡션으로 남긴다.

### 3.6 표

- 얼룩 배경(zebra) 금지. 행 구분은 여백과 `#dedee5` 1px 선으로.
- 합계 행은 위에 2px `#101114` 선을 긋고 굵게.
- hover는 배경색 변화 하나만. 이동·확대·그림자 금지.

### 3.7 차트

- 계열 색에 등락 빨강/파랑을 재사용하지 않는다. 중립 계열 4~5단계를 쓰고, 강조 한 계열만 보라.
- 비중 표현은 도넛보다 가로 누적 막대를 우선한다.
- 차트마다 동일 데이터를 담은 표 대안을 제공한다.

### 3.8 모션

원본에 규정이 없다. 다음만 허용한다.

| 허용 | 설정 |
| --- | --- |
| 값 갱신 | 숫자 전환 400ms, 등락 표식 페이드 200ms |
| 패널 열기/닫기 | 180ms ease-out |
| 행 hover | 120ms |
| 동기화 진행 | 상단 바 얇은 진행 표시 하나 |

`prefers-reduced-motion: reduce`에서는 숫자 전환과 진행 표시를 끄고 최종 상태를 즉시 보여준다.

---

## 4. 금지

- 상승을 초록으로 표시하는 모든 구현
- 등락 표시에 브랜드 보라 사용
- 원본에 정의되지 않은 보라 계열 추가
- pill 버튼 (원본 Don't 유지)
- 원본 그림자 2종 외의 그림자 추가
- 표 안에서 금액 축약, 소수점 자리 들쭉날쭉
- 7px 마이크로 텍스트, 대문자 한글/영문 라벨
- 수익률 강조 배지, 축하 연출, 불꽃·로켓류 아이콘
- 오류 문구에 기술 용어(RLS, 401, snapshot job) 노출

---

## 5. 토큰

원본 색은 Kraken 이름을 유지하고, 프로젝트 추가분만 `--qm-` 접두사를 쓴다. 어느 쪽에서 온 값인지 코드에서 바로 구분하기 위해서다.

```css
:root {
  /* Kraken DESIGN.md 원본 */
  --kraken-purple: #7132f5;
  --kraken-purple-dark: #5741d8;
  --kraken-purple-deep: #5b1ecf;
  --kraken-purple-subtle: rgba(133, 91, 251, 0.16);
  --kraken-near-black: #101114;
  --kraken-cool-gray: #686b82;
  --kraken-silver-blue: #9497a9;
  --kraken-border: #dedee5;
  --kraken-surface: #ffffff;
  --kraken-green: #149e61;      /* 시스템 성공 전용. 등락 금지 */
  --kraken-green-dark: #026b3f;

  --kraken-shadow-subtle: rgba(0, 0, 0, 0.03) 0 4px 24px;
  --kraken-shadow-micro: rgba(16, 24, 40, 0.04) 0 1px 4px;

  /* QUANT-MIND 추가 */
  --qm-rise: #D42E3C;
  --qm-rise-text: #B21F2C;
  --qm-fall: #1B6FD4;
  --qm-fall-text: #1558AA;
  --qm-flat: #686b82;
  --qm-stale: #8A8F86;
  --qm-warn: #B4762A;
  --qm-danger: #8E2C24;

  --qm-font: "Pretendard Variable", system-ui, -apple-system,
             "Apple SD Gothic Neo", "Malgun Gothic", sans-serif;
  --qm-leading-body: 1.6;
  --qm-leading-read: 1.8;
}
```

하드코딩된 hex를 컴포넌트에 쓰지 않는다. 모든 색은 위 변수로만 참조한다.

---

## 6. constitution 추가 조항

```
1. 프로젝트 루트의 DESIGN.md(Kraken)를 UI 기본 시각 기준으로 사용한다.
2. DESIGN.md 원본 파일은 수정하지 않는다.
3. DESIGN.quantmind.md는 DESIGN.md보다 우선한다. 두 문서가 충돌하면
   DESIGN.quantmind.md를 따르고, 충돌 사실을 해당 문서에 기록한다.
4. 등락 표시는 상승 빨강 / 하락 파랑을 따른다. 초록은 시스템 성공 상태에만 쓴다.
5. 전용 상표 폰트와 브랜드 자산은 복제하지 않고 대체 폰트를 사용한다.
6. 모든 수치는 tabular figures, 우측 정렬, 고정 소수점 자리로 표시한다.
```

---

## 7. analyze 단계 검증 목록

```
DESIGN.md 원본이 수정되지 않았는가
상승이 초록으로 구현되지 않았는가
Kraken 초록이 등락 표시에 쓰이지 않았는가
보라가 등락 표시에 쓰이지 않았는가
정의되지 않은 보라 계열이 추가되지 않았는가
Kraken 전용 폰트를 그대로 참조하지 않았는가
한글 제목에 px 단위 음수 자간이 그대로 들어가지 않았는가
본문 행간이 1.6 이상인가
모든 수치에 tabular-nums가 적용되었는가
모든 수치 블록에 기준 시각이 있는가
pill 버튼이 없는가, 버튼 반경이 12px인가
원본 2종 외의 그림자가 추가되지 않았는가
차트에 표 대안이 있는가
prefers-reduced-motion 처리가 있는가
```

---

## 8. 미결정 사항 — 추가 확인 필요

- **읽기용 명조체 도입 여부.** 지난 장부형 초안에는 결정 기록 본문에 Noto Serif KR을 두었다. Kraken은 sans 2종 체계라 명조를 넣으면 원본과 어긋난다. 현재 문서는 **넣지 않는 쪽**으로 작성했다. 장문 기록의 가독성을 위해 되살릴지 결정 필요.
- **야간 모드.** Kraken 원본은 화이트 기준이며 다크 팔레트가 없다. 필요하면 이 문서에 별도 팔레트를 정의해야 한다. 현재 미정의.
- **색 대비 실측.** `--qm-rise-text`, `--qm-fall-text`, 보라 `#7132f5`를 흰 배경에 올렸을 때 4.5:1 충족 여부 측정 필요.
- **보라 사용 면적.** 가계 장부 성격에 비해 원본 브랜드 보라가 강하다. CTA와 선택 상태로만 제한할지, 헤더까지 확장할지 결정 필요.
- **폰트 조달 방식.** Pretendard 셀프 호스팅 / CDN 중 선택, 저장소 포함 시 라이선스 확인.
